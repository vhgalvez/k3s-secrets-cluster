# vars/main.yml

# 🔐 Credenciales para autenticación htpasswd (auth_secrets.yml y generate_base_secrets.yml)
common_auth_user: admin

prometheus_auth_password: prom123
grafana_auth_password: graf456
jenkins_auth_password: jenk789
argocd_auth_password: argopass
longhorn_auth_password: longpass

# 📦 Ruta al certificado público de Sealed Secrets (exportado con kubeseal --fetch-cert)
pub_cert_path: /path/to/pub-cert.pem

# 📘 Kubeconfig para interactuar con el clúster
kubeconfig_path: "/home/victory/.kube/config"

# 📥 Configuración del Helm Chart de Sealed Secrets
sealed_secrets_namespace: "kube-system"
sealed_secrets_release_name: "sealed-secrets"
sealed_secrets_chart_version: "2.15.3"
